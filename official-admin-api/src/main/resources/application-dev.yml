spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/official_admin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8&createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    show-sql: false

logging:
  level:
    root: INFO
    com.company.admin: INFO
