spring:
  datasource:
    url: jdbc:mysql://localhost:3306/official_admin?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
logging:
  level:
    root: INFO
    com.company.admin: DEBUG
